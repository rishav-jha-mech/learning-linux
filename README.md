# learning-linux

Notes from learning Linux, organized roughly by difficulty:

- `basic/` — shell basics, files, permissions, everyday commands
- `intermediate/` — processes, networking, shell scripting
- `advanced/` — internals, debugging, containers

Rendered for browsing via the Docusaurus site in `site/`.
